---
name: example
description: Example command for the dev-forge plugin
---

This is an example slash command. Replace this with your actual command implementation.

When a user types `/example`, this prompt will be expanded and sent to Claude.
