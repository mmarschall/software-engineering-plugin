# Scripts

This directory contains executable scripts referenced by hooks and other plugin utilities.

Scripts can be shell scripts, Python scripts, or any other executable that supports your plugin's functionality.
